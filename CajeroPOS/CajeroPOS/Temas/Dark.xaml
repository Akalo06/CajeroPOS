<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <!-- Fondo principal (gris medio claro) -->
    <Color x:Key="BgColor">#FF3A3A3A</Color>

    <!-- Paneles (ligeramente más claros) -->
    <Color x:Key="PanelColor">#FF4A4A4A</Color>

    <!-- Acento (azul moderno) -->
    <Color x:Key="AccentColor">#FF4C8DFF</Color>

    <!-- Texto (gris muy claro, no blanco puro) -->
    <Color x:Key="TextColor">#FFEDEDED</Color>

    <!-- Brushes -->
    <SolidColorBrush x:Key="BgBrush" Color="{StaticResource BgColor}"/>
    <SolidColorBrush x:Key="PanelBrush" Color="{StaticResource PanelColor}"/>
    <SolidColorBrush x:Key="AccentBrush" Color="{StaticResource AccentColor}"/>
    <SolidColorBrush x:Key="TextBrush" Color="{StaticResource TextColor}"/>

</ResourceDictionary>